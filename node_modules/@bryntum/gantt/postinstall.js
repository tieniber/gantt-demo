const
    { spawn } = require('child_process'),
    pkg       = '@bryntum/gantt-trial-lib',
    script    = `${pkg}/build.js`;
try {
    spawn('node', [require.resolve(script)], { cwd : process.cwd(), stdio : 'inherit' });
}
catch (e) {
    console.log(`Optional ${pkg} is not installed`);
}
